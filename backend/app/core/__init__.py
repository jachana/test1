"""Core domain logic: astronomy, projection, artwork, validation.

This package is deliberately free of FastAPI / HTTP concerns so it can be
unit-tested and reused independently of the web layer.
"""
