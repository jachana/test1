"""Service layer: orchestration, geocoding, artwork assembly.

Keeps astronomy, projection, SVG serialisation, and export conversion as
separate stages so each can be tested and cached independently.
"""
